
function listAllProduct() {
    //when the document.onReady it will need to fetch all of the SKU's from the database via PHP Script
}

function addProduct() {
    //validate the fields are valid.
    //then passes the input data to the php script via POST
}

function massDelete() {
    //check which checkbox value evaluates to true and record those SKU's in an array. 
    //POST the SKU's as a variable to the php script that need to be deleted. 
}
